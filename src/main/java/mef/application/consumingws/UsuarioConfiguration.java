package mef.application.consumingws;

//import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
//import org.springframework.oxm.jaxb.Jaxb2Marshaller;

@Configuration
public class UsuarioConfiguration {
	/*
	@Bean
	public Jaxb2Marshaller marshaller() {
		Jaxb2Marshaller marshaller = new Jaxb2Marshaller();
		marshaller.setContextPath("mef.application.consumingws.wsdl");
		return marshaller;
	}
	
	@Bean
	public UsuarioClient validaUsuario(Jaxb2Marshaller marshaller) {
		System.out.println("Entro 1");
		UsuarioClient client = new UsuarioClient();
		client.setDefaultUri("http://linksoft-desarrollo.sytes.net/WS_Seguridad/WSSeguridad.svc");
		client.setMarshaller(marshaller);
		client.setUnmarshaller(marshaller);
		return client;
	}
*/
}
